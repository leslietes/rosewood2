  <header class="header_bg clearfix">
  <div class="container clearfix">
    <nav>

      <!-- Logo -->
      <div class="logo">
        <a href="index.html"><%= image_tag @setting.logo.url(:medium)%></a>
      </div>
      <!-- /Logo -->

      <!-- Master Nav -->
      <nav class="main-menu">
        <ul>
          <li><%= link_to 'Home', root_url %></li>
          <li><%= link_to 'Location', location_url %></li>
          <li><%= link_to 'FAQ', faqs_url %></li>
        	<li><%= link_to 'Contact', contact_url %></li>
          <% if current_user %>
            <li><%= link_to 'SIGN OUT', destroy_user_session_path, :method => :delete %></li>
          <% end %>  
         </ul>
       </nav>
       <!-- /Master Nav -->

    </div>
  </header>

  <div class="clear"></div>